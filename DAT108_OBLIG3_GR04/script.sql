set search_path = oblig3;

CREATE TABLE Deltaker 
(
   mobil	VARCHAR(8),
   passord	CHAR(100),
   fornavn	VARCHAR(100),
   etternavn	VARCHAR(100),
   kjonn	VARCHAR(10),
   
   PRIMARY KEY (mobil)
);


	
